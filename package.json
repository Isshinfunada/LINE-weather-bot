{
  "name": "line-weather-bot",
  "version": "1.0.0",
  "description": "LINE Weather Bot using LINE MCP Server for rainy day notifications (Bun + Hono)",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "vercel-dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "node-cron": "^3.0.3"
  },
  "optionalDependencies": {
    "@line/line-bot-mcp-server": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.0",
    "bun-types": "^1.0.0"
  },
  "keywords": ["LINE", "bot", "weather", "MCP", "bun", "hono"],
  "author": "",
  "license": "ISC"
}